# Example local.properties for experiment_4
# Copy this file to local.properties (in repo root or experiment_4 root) and replace the value with your real API key.
# Do NOT commit the real local.properties to version control.

OPENWEATHER_API_KEY=YOUR_API_KEY_HERE
