// Top-level build file for experiment_4 (keeps minimal configuration)
plugins {
    // kept empty here; the module will apply the Android plugin
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
